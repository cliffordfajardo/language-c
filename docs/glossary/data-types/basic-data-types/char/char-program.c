#include <stdio.h> 
#include <stdlib.h>

int main(){
  char my_char = 'A';
  printf("ch = %c, (interpreted as a char) \n",my_char);
  printf("ch = %d, (interpretted as an integer)\n",my_char);
  return 0;
}